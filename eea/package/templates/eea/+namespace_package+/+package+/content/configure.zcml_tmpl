<configure
    xmlns="http://namespaces.zope.org/zope"
    i18n_domain="${namespace_package}">

  <class class=".${ctype_module}.${ctype}">
    <implements interface=".interfaces.I${ctype}" />
  </class>

</configure>
