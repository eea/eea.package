""" Public interfaces
"""
# Browser layer
from ${project}.browser.interfaces import ILayer

# Content
from ${project}.content.interfaces import I${ctype}

# Control Panel
from ${project}.controlpanel.interfaces import ISettings

__all__ = [
    ILayer.__name__,
    I${ctype}.__name__,
    ISettings.__name__,
]
