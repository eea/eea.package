<configure
  xmlns="http://namespaces.zope.org/zope"
  i18n_domain="${namespace_package}">

  <permission
    id="${project}.add"
    title="${project}: Add"
    description="Add ${title}"
    />

</configure>
